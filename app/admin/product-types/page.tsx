import { Placeholder } from "@/components/ui/Placeholder";

export default function AdminProductTypesPage() {
  return (
    <Placeholder
      title="Product types"
      description="Managed list (vinyl, tape, …) — add, rename, delete (guarded if products are linked). CRUD coming soon."
    />
  );
}
