import { Placeholder } from "@/components/ui/Placeholder";

export default function AdminLabelsPage() {
  return (
    <Placeholder
      title="Labels"
      description="Managed list — add, rename, delete (guarded if products are linked). CRUD coming soon."
    />
  );
}
