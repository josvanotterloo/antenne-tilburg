# Session Log — 2026-07-01

## What was built
- Full initial scaffold of the Antenne Tilburg site (feature 001): Next.js 14
  App Router + TypeScript, Prisma schema for all 12 entities, NextAuth
  credentials auth with `/admin/*` protection, seed with two admins, admin +
  public placeholder shells, notices banner, live-hours footer, robots/sitemap.
- Ran the `branching.md` close-out: `/code-review`, fixed 2 MEDIUM+ issues,
  fast-forward merged `feature/initial-scaffold` → `master`, deleted the branch,
  pushed to the new GitHub remote.

## What worked
- Hand-scaffolding (vs `create-next-app`) gave exact control over the required
  structure; build was green first try.
- Graceful DB-read degradation let `next build` pass with no live Postgres —
  26/26 pages prerendered.
- Code review caught a real functional bug (middleware redirecting to the
  default NextAuth page instead of `/admin/login`).

## What drifted from intent
- User expected a checkpoint: asked "did you already create code?" after I had
  executed the whole kickoff. The kickoff read as a step-by-step "do this" spec
  and the prompt said "follow it step by step," so I treated it as authorization
  — but a one-line "executing now, not just planning" up front would have set
  expectations.

## Signal (what should change in a shared artifact)
- [x] Instruction: kickoff-style "do this" docs should state explicitly whether
      they want a plan-first checkpoint or straight execution.
- [ ] Context:
- [ ] Workflow:
- [ ] Failure:
- [ ] None

## Friction points
- **Correction:** user checked whether code had already been written.
  **Prompt that would have prevented it:** "Execute the kickoff end-to-end
  without stopping for approval; tell me up front you're building, not planning."
- Environment: no live Postgres, so `prisma migrate`/seed and DB-dependent
  tests could not run — flagged, not resolved.

## Updates made
- `CLAUDE.md`, `docs/instructions/generate-route.md`, `.env.example`,
  `tasks/todo.md`, `.gitignore` (Next/OS/env ignores).
- New: `docs/features/001-initial-scaffold.md`, this session log.
- Fixes: `middleware.ts`, `NoticeBanner.tsx`, `SiteFooter.tsx`.
