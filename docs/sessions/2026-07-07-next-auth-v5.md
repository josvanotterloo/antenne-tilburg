# Session Log — 2026-07-07 (next-auth v5)

## What was built
- Migrated next-auth v4 → v5 (Auth.js). Split config: lib/auth.config.ts (Edge-safe) for
  the middleware; lib/auth.ts (Node, Credentials + db) exports handlers/auth/signIn/signOut.
  Route, api-auth (auth()), proxy middleware, and AdminTopNav (redirectTo) updated.

## What worked
- The split config kept Prisma out of the Edge middleware — next build compiled the
  middleware cleanly.
- Passing `secret` in the config (from NEXTAUTH_SECRET) + trustHost avoided any .env
  change (no AUTH_SECRET rename needed).
- Live endpoint checks (providers/csrf/session + the /admin 307 redirect) verified the
  setup without needing to log in.

## What drifted from intent
- Couldn't submit the login form (no password entry), so the credential sign-in flow
  itself is unverified end-to-end — flagged for a human to do one real login. The
  authorize logic is unit-tested and unchanged.
- next-auth v5 is still labeled beta (standard for Next 16).

## Signal (what should change in a shared artifact)
- [ ] None

## Updates made
- package.json/lock (next-auth v5), lib/auth.config.ts (new), lib/auth.ts, auth route,
  lib/api-auth.ts (+test), proxy.ts, AdminTopNav, feature doc, this log, backlog.
